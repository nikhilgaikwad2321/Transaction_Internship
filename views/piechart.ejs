<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Category Statistics</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .card {
            background-color: yellowgreen;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 300px;
        }
        .card-title {
            font-size: 24px;
            margin-bottom: 10px;
            color: #333333;
        }
        .category {
            margin-bottom: 10px;
        }
        .category label {
            font-weight: bold;
            color: #555555;
        }
        .category-value {
            font-size: 18px;
            color: #007bff;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1 class="card-title">Categories for Month</h1>
        
        <% response.categories.forEach(category => { %>
        <div class="category">
            <label><%= category.category %>:</label>
            <p class="category-value"><%= category.count %></p>
        </div>
        <% }); %>
        
    </div>
</body>
</html>
